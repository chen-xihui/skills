# 场景 4：主从数据不一致

**症状**：从节点数据与主节点不一致

**诊断步骤**：
1. 执行 `paas-cli redis nodes` 查看主从状态
2. 扁鹊诊断检查 replication

**常见原因与处理**：

| 原因 | 处理建议 |
|------|---------|
| 网络延迟导致同步慢 | 检查主从节点间网络 |
| 主节点写入量过大 | 考虑分片或读写分离 |
| 从节点断开后重连 | 等待全量同步完成 |
| 从节点只读配置异常 | 确认从节点为只读模式 |
