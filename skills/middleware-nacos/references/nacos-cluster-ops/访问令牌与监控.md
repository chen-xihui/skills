# 六、Nacos 访问令牌与监控

## 操作 18: 创建访问令牌

| 属性 | 说明 |
|------|------|
| 操作类型 | 创建访问令牌 |
| resource | `nacosclusteraccesstoken` |
| action | `create` |
| 风险等级 | 🟡 中风险 |
| 需确认 | 是 |

```bash
paas-cli create nacosclusteraccesstoken --gateway-config=config/gateway.yaml -f config/nacos/iteration-nacos-cluster-access-token-create.yaml
```

---

## 操作 19: 查询访问令牌

| 属性 | 说明 |
|------|------|
| 操作类型 | 查询访问令牌 |
| resource | `nacosclusteraccesstoken` |
| action | `get` |
| 风险等级 | 🟢 低风险 |
| 需确认 | 否 |

```bash
paas-cli get nacosclusteraccesstoken --gateway-config=config/gateway.yaml -f config/nacos/iteration-nacos-cluster-access-token-get.yaml
```

---

## 操作 20: 删除访问令牌

| 属性 | 说明 |
|------|------|
| 操作类型 | 删除访问令牌 |
| resource | `nacosclusteraccesstoken` |
| action | `delete` |
| 风险等级 | 🟡 中风险 |
| 需确认 | 是 |

```bash
paas-cli delete nacosclusteraccesstoken --gateway-config=config/gateway.yaml -f config/nacos/iteration-nacos-cluster-access-token-delete.yaml
```

---

## 操作 21: 开启/关闭监控

| 属性 | 说明 |
|------|------|
| 操作类型 | 开启/关闭监控 |
| resource | `nacosclustermonitor` |
| action | `update` |
| 风险等级 | 🟢 低风险 |
| 需确认 | 否 |

```bash
paas-cli update nacosclustermonitor --gateway-config=config/gateway.yaml -f config/nacos/iteration-nacos-cluster-monitor-update.yaml
```

---

## 操作 22: 查询监控信息

| 属性 | 说明 |
|------|------|
| 操作类型 | 查询监控信息 |
| resource | `nacosclustermonitor` |
| action | `get` |
| 风险等级 | 🟢 低风险 |
| 需确认 | 否 |

```bash
paas-cli get nacosclustermonitor --gateway-config=config/gateway.yaml -f config/nacos/iteration-nacos-cluster-monitor-get.yaml
```