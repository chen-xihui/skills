# 五、Nacos 网络与访问控制

## 操作 14: 创建网络策略

| 属性 | 说明 |
|------|------|
| 操作类型 | 创建网络策略 |
| resource | `nacosclusternetworkpolicy` |
| action | `create` |
| 风险等级 | 🟡 中风险 |
| 需确认 | 是 |

```bash
paas-cli create nacosclusternetworkpolicy --gateway-config=config/gateway.yaml -f config/nacos/iteration-nacos-cluster-network-policy-create.yaml
```

---

## 操作 15: 查询网络策略

| 属性 | 说明 |
|------|------|
| 操作类型 | 查询网络策略 |
| resource | `nacosclusternetworkpolicy` |
| action | `get` |
| 风险等级 | 🟢 低风险 |
| 需确认 | 否 |

```bash
paas-cli get nacosclusternetworkpolicy --gateway-config=config/gateway.yaml -f config/nacos/iteration-nacos-cluster-network-policy-get.yaml
```

---

## 操作 16: 更改网络策略

| 属性 | 说明 |
|------|------|
| 操作类型 | 更改网络策略 |
| resource | `nacosclusternetworkpolicy` |
| action | `update` |
| 风险等级 | 🟡 中风险 |
| 需确认 | 是 |

```bash
paas-cli update nacosclusternetworkpolicy --gateway-config=config/gateway.yaml -f config/nacos/iteration-nacos-cluster-network-policy-update.yaml
```

**确认流程**：网络策略变更可能影响客户端连接，需确认影响范围后执行。

---

## 操作 17: 删除网络策略

| 属性 | 说明 |
|------|------|
| 操作类型 | 删除网络策略 |
| resource | `nacosclusternetworkpolicy` |
| action | `delete` |
| 风险等级 | 🔴 高风险 |
| 需确认 | 是 |

```bash
paas-cli delete nacosclusternetworkpolicy --gateway-config=config/gateway.yaml -f config/nacos/iteration-nacos-cluster-network-policy-delete.yaml
```

**注意**：删除网络策略可能导致集群暴露给未授权访问。